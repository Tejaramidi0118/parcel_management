// Dummy Prisma export for backward compatibility
// We're now using direct PostgreSQL pool from db.config.js
// This prevents import errors in files that still reference prisma

export default {
    // Add stubs as needed
};
